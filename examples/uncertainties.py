"""
Analyze empirical coverages of the confidence intervals predicted by
the model vs other models. Uses simple distance measures (PICA), as well
as metrics based on the entire predictive distributions (IS, WIS).

Compares with BART, Bayesian Lin. Regression, GP
NGBoost, quantile methods (RF, Boost), distributional forest,
split conformal regression (mapie) with e.g. XGBoost,
e.g. bootstrapping prediction data with XGBoost"""
